# name      access  trigger   handles   prec
# dispatch comes from the project's machine.json (state -> role); `handles` is unused.
qa          review  reactive  -         10
engineer    edit    reactive  -         20
lead        draft   every:24h -         30
